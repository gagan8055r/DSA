  char name[]="gagan";
  int i,count;

for(i=0;name[i]!='\0';i++)
{
    if(name[i]='a' || name[i]='e' || name[i]='i' || name[i]='o' || name[i]='u' ) ////vowels in the words
    {
        count++;
        
    }
}
cout<<count<<endl;
